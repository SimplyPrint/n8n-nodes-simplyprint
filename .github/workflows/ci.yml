name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-and-build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        # 22.x is current LTS; 24.x is what the release workflow publishes under
        # (npm 11+ for Trusted Publishing). Keep both green so a Node-24-only
        # regression doesn't surface at release time.
        node-version: [22.x, 24.x]
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      # --ignore-scripts skips native builds of transitive n8n deps
      # (e.g. isolated-vm). We only need n8n-workflow for type-checking.
      - run: npm ci --ignore-scripts

      - run: npm run lint

      - run: npm test

      - run: npm run build

      - name: Verify package can be packed
        run: npm pack --dry-run
